import React, {useState} from "react";
import { useNavigate } from "react-router-dom";
import Header from "../Header";
import Sidebar from "../Sidebar";

const Varients = () => {
    return(
        <>
            <h1>This is varients component</h1>
        </>
    )
}

export default Varients;
